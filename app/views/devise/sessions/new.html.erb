<div id = "intro"> 
  <h3> WordOut </h3> 
  <p> 
    WordOut is a platform for poets to share their work without criticism or judgment. </br> 
    Feel free to write about gender, race, social issues, mental/personal health, identity, anything at all. </br> 
    This is a safe space. One love. 
  </p> 
</div> 

<div id="login"> 
<h2>Log in</h2>

<%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
  <div class="field">
    <%= f.label :email %><br />
    <%= f.email_field :email, autofocus: true %>
  </div>

  <div class="field">
    <%= f.label :password %><br />
    <%= f.password_field :password, autocomplete: "off" %>
  </div>

  <% if devise_mapping.rememberable? -%>
    <div class="field">
      <%= f.check_box :remember_me %>
      <%= f.label :remember_me %>
    </div>
  <% end -%>

  <div class="actions">
    <%= f.submit "Log in", :class => "btn btn-default" %>
  </div>
<% end %>

<%= render "devise/shared/links" %>

</div> 

