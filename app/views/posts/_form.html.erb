<%= form_for(@post) do |f| %>
	<p>
		<%= f.label(:title) %>
		<%= f.text_field(:title, size: "40") %>
	</p>
	<p>
		<%= f.label(:text) %>
		<%= f.text_area(:text, size: "100x20") %>
	</p>
	<p>
		<%= f.submit "Publish", :class => "btn btn-success" %>
	</p>
<% end %>
